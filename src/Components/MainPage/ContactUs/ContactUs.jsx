import {Button} from "react-bootstrap";
const ContactUs = () => {
    return (
        <Button variant="dark" size="lg">
            Contáctanos
        </Button>
    );
};

export default ContactUs;
